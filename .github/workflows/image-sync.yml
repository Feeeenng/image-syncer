# This is a basic workflow to help you get started with Actions

name: image-sync
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 14 * * 1'
  issues:
    types:
      - opened
env:
  IMAGE_SYNCER_VERSION: v1.3.1
  USERNAME: ${{ secrets.ALIYUN_USERNAME }}
  PASSWORD: ${{ secrets.ALIYUN_PASSWORD }}
  ISSUSE_BODY: ${{ github.event.issue.body }}

jobs:
  image-sync:
    runs-on: ubuntu-latest
    
    if: contains(github.event.issue.labels.*.name, 'image-syncer')
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: install image-sync tool
      run: |
        wget https://github.com/AliyunContainerService/image-syncer/releases/download/${IMAGE_SYNCER_VERSION}/image-syncer-${IMAGE_SYNCER_VERSION}-linux-amd64.tar.gz
        tar -zxf image-syncer-${IMAGE_SYNCER_VERSION}-linux-amd64.tar.gz
        
    - name: add sercret to file
      run: envsubst < config_source.yaml > config.yaml
    

    - name: add image file
      run: envsubst < image_file.yaml > image_file.yaml

    - name: cat file
      run: cat image_file.yaml
    
    - name: sync images
      run: ./image-syncer --proc=10 --config=config.yaml --retries=2 >> output.sh
     # run: ./image-syncer --proc=10 --auth=config.yaml  --images=image_file.yaml
    
 #   - name: Add comment
 #       if: ${{ hashFiles('output.sh') }}
 #       uses: actions/github-script@v5
 #       with:
 #        sciprt: ./image-syncer --proc=10 --auth=config.yaml  --image=image.file.yaml
            
